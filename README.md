# Quote API

A simple REST API built with Flask that returns a random inspirational quote as JSON.

## Endpoints
- GET / — welcome message
- GET /quote — returns a random quote in JSON format

## Requirements
- Python 3
- Flask (pip install flask)

## Usage
